## Faster RWKV Python Binding

### Installation

```bash
pip install fasterrwkv
```

### Development

In this directory, run

```bash
pip install -e .
```

NOTE: DO NOT run `python3 setup.py develop` because we are using pyproject.toml.
